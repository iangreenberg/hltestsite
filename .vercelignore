# Ignore node_modules (Vercel handles these)
node_modules

# Ignore development-only files
README.md
.git
.gitignore
.breakpoints
.replit

# Don't need to upload client source as we're building it during deployment
client/src
client/public

# Don't need to upload server source as we're building it during deployment
server

# Vite config
vite.config.ts

# Other configuration files we don't need in deployment
tsconfig.json
postcss.config.js
tailwind.config.ts
theme.json

# Don't ignore the built files
!dist
!api
{
  "name": "client",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "cd .. && npm install && cd client && vite build",
    "vercel-build": "cd .. && npm install && cd client && vite build"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}